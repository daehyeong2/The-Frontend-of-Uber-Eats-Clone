const colors = require("tailwindcss/colors");

/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    content: [],
    extend: {
      colors: {
        ...colors,
      },
    },
    fontFamily: {
      freesentation: ["freesentation"],
    },
  },
  plugins: [],
};
